replicaCount: 2

image:
  repository: brewmind-ai
  tag: "1.0.0"
  pullPolicy: Never

service:
  type: NodePort
  port: 80
  targetPort: 8080
  nodePort: 30080

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"

env:
  claudeApiKey: ""
