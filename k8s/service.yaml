apiVersion: v1
kind: Service
metadata:
  name: nova-ai-store-service
spec:
  selector:
    app: nova-ai-store
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30080
  type: NodePort
