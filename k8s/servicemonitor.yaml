apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nova-ai-store-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: nova-ai-store
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 15s
