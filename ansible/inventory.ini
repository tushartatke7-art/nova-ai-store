# ── NOVA AI Store — Ansible Inventory ────────────
# This file lists all servers Ansible will manage
# For now we use localhost (our own machine) for testing
# In production, you'd put your real server IPs here

[nova_servers]
localhost ansible_connection=local

# Example of real servers (commented out):
# 192.168.1.100 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
# 192.168.1.101 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
