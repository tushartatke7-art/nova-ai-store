version: '3.8'
services:
  app:
    build: .
    container_name: nova-app
    ports:
      - "8080:8080"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
    networks:
      - nova-network

networks:
  nova-network:
    driver: bridge
